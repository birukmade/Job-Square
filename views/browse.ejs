<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("./includes/head") %>
  </head>
  <body>
    <%- include ("./includes/header") %> <%- include ("./includes/side-drawer")
    %>
    <main>
      <div class="container">
        <% if (numberOfJobs === 0) { %>
        <h2 class="about-title">No jobs found</h2>
        <p>Unfortunately, No Jobs are posted yet. Try again latter</p>
        <% } else {%> <% for (const job of jobs) { %> <%- include
        ("./includes/job-item", {job:job}) %> <% } %> <% } %>
      </div>
    </main>

    <%- include ("./includes/footer") %>
  </body>
</html>
